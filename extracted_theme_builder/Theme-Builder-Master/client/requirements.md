## Packages
framer-motion | Page transitions and scroll-triggered animations
lucide-react | Scalable SVG icons

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}

Colors - The app uses a forced dark mode "Obsidian" theme globally via index.css.
Custom values like "ember gold" and "storm violet" are mapped to primary and secondary.
