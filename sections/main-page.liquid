<section class="page-width section-{{ section.id }}">
  <h1>{{ shop.name }}</h1>
  <p>This theme scaffold was added so this branch is recognized as a valid Shopify theme branch.</p>
</section>

{% schema %}
{
  "name": "Main page",
  "settings": []
}
{% endschema %}
